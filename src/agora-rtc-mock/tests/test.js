import expect from 'expect'

describe('======= Sanity Agora RTC Mock =======', () => {
  // eslint-disable-next-line no-undef
  it('Should be sane', (done) => {
    expect(true).toEqual(true)
    done()
  })
})
